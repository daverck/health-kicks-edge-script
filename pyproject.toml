[project]
name = "healthkicks_edge"
version = "0.1.0"
description = "Serial-to-MQTT bridge for the HealthKicks Raspberry Pi edge service"
requires-python = ">=3.10"
dependencies = [
    "paho-mqtt>=2.0,<3.0",
    "pyserial>=3.5,<4.0",
]

[tool.uv]
package = false